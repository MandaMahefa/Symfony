<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">

        <link rel="apple-touch-icon" sizes="180x180" href="https://symfony.com/favicons/apple-touch-icon.png?">
        <link rel="icon" type="image/png" sizes="16x16" href="https://symfony.com/favicons/favicon-16x16.png?">
        <link rel="icon" type="image/png" sizes="32x32" href="https://symfony.com/favicons/favicon-32x32.png?">

        <title>{% block title %}Mon Super Agence{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    <nav class=" navbar-default bg-light mt-4" >
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand navlink {% if  current_menu is defined and current_menu == "home" %} active {% endif %}" href="{{ path('home') }}">Mon Agence</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="navlink {% if  current_menu is defined and current_menu == "properties" %} active {% endif %}"><a href="{{ path('property_index') }}">Les biens <span class="sr-only">(current)</span></a></li>

                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#">Etat</a></li>
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('logout') }}">Se déconnecter</a>
                        </li>
                    {% endif %}

                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
        <!-- jQuery library -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

        <!-- Latest compiled JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    </body>

</html>
